fastapi[standard]>=0.115.0
uvicorn
sqlalchemy
pydantic
pydantic[email]
python-jose[cryptography]
bcrypt>=4.0
alembic
openai
python-multipart
aiosqlite
asyncpg
python-dotenv

# Optional: required only when REDIS_URL is set (multi-worker rate limiting).
redis

# Testing
pytest
httpx
