# Copy this file to `.env.local` and adjust as needed.

# Base URL of the FastAPI backend. Must point to the same origin in production
# (or use a Next.js rewrite so requests stay same-origin).
NEXT_PUBLIC_API_URL=http://localhost:8000
